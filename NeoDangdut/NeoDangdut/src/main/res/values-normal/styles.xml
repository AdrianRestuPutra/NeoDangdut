<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/main_bg</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="menu_labels_style">
        <item name="android:background">@drawable/fab_label_background</item>
        <item name="android:textColor">@color/white</item>
    </style>

    <style name="navigation_view">
        <item name="android:listDivider">@color/tab_divider</item>
    </style>

    <style name="CustomHomeTab" parent="TextAppearance.Design.Tab">
        <item name="android:textSize">@dimen/tab_text_medium_size</item>
    </style>

    <style name="CustomLibraryTab" parent="TextAppearance.Design.Tab">
        <item name="android:textSize">@dimen/tab_text_large_size</item>
    </style>

    <style name="CustomShopTab" parent="TextAppearance.Design.Tab">
        <item name="android:textSize">@dimen/tab_text_size</item>
    </style>

</resources>
